# Postgres
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme
POSTGRES_DB=postgres

# JWT shared by identity-service, admin-bff, and api-gateway (key must be >= 32 chars)
JWT_SECRET_KEY=replace-with-strong-random-string-min-32-chars
JWT_ISSUER=tickethub-issuer
JWT_AUDIENCE=tickethub-audience
